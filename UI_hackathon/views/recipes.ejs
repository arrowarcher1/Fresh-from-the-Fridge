<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recipes · Fresh from the Fridge</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
    .recipe-card { cursor: pointer; transition: all 0.2s; }
    .recipe-card:hover { transform: translateY(-2px); box-shadow: 0 20px 25px -5px rgba(0,0,0,0.1), 0 10px 10px -5px rgba(0,0,0,0.04); }
    .recipe-instructions { display: none; }
    .recipe-card.expanded .recipe-instructions { display: block; }
    .recipe-card.expanded .recipe-preview { display: none; }
  </style>
  <script>
    function toggleRecipe(id) {
      const card = document.getElementById('recipe-' + id);
      if (card) card.classList.toggle('expanded');
    }
  </script>
</head>
<body class="min-h-screen text-gray-900" style="background: radial-gradient(circle at top left, rgba(167,243,208,0.45), rgba(240,253,250,0.9)), linear-gradient(135deg, rgba(217,249,157,0.45) 0%, rgba(255,255,255,0.8) 60%, rgba(187,247,208,0.5) 100%);">
  <header class="sticky top-0 z-30 bg-white bg-opacity-95 border-b border-emerald-100 shadow-sm">
    <div class="max-w-5xl mx-auto px-6 py-3 flex items-center justify-between">
      <a href="/" class="flex items-center gap-3">
        <span class="inline-flex h-9 w-9 items-center justify-center rounded-lg bg-gradient-to-br from-emerald-500 to-teal-500 text-xs font-semibold uppercase text-white">FF</span>
        <span class="text-lg font-bold tracking-tight text-emerald-900">Fresh from the Fridge</span>
      </a>
      <nav class="flex items-center gap-1 text-sm">
        <a href="/" class="inline-flex items-center px-3 py-2 rounded-lg text-emerald-700 hover:bg-emerald-50 hover:text-emerald-900">Fridge</a>
        <a aria-current="page" href="/recipes" class="inline-flex items-center px-3 py-2 rounded-lg font-medium text-emerald-900 bg-emerald-100 border border-emerald-200 hover:bg-emerald-200 hover:border-emerald-300 transition">Suggested Recipes</a>
        <a href="/upload" class="inline-flex items-center px-3 py-2 rounded-lg text-emerald-700 hover:bg-emerald-50 hover:text-emerald-900">Upload Receipt</a>
      </nav>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-6 py-10 space-y-6">
    <section class="grid sm:grid-cols-2 gap-4">
      <div class="text-white rounded-xl shadow-md p-5" style="background: linear-gradient(135deg, #34d399, #4ade80 40%, #22d3ee 100%);">
        <p class="text-sm uppercase tracking-widest font-semibold mb-2">Matched dishes</p>
        <p class="text-xl font-semibold mb-1">Curated for your fridge</p>
        <p class="text-sm text-emerald-50">We score recipes based on ingredients you already have.</p>
      </div>
      <div class="bg-white rounded-xl border border-emerald-100 shadow-sm p-5" style="background: linear-gradient(145deg, rgba(255,255,255,0.95), rgba(240,253,244,0.9));">
        <div class="text-sm uppercase tracking-widest text-emerald-500 font-semibold mb-2">At a glance</div>
        <p class="text-emerald-900 text-3xl font-semibold mb-1"><%= (recipes && recipes.length) ? recipes.length : 0 %> suggestions</p>
        <p class="text-sm text-emerald-700">Ranked by ingredient overlap.</p>
      </div>
    </section>

    <div class="flex items-center justify-between mb-4">
      <h2 class="text-xl font-semibold text-emerald-900">Suggested Recipes</h2>
      <a href="/" class="text-sm font-semibold text-emerald-700 hover:text-emerald-900">← Back to Fridge</a>
    </div>

    <% if (recipes && recipes.length) { %>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        <% recipes.forEach(function(r, idx){ %>
          <article id="recipe-<%= idx %>" onclick="toggleRecipe(<%= idx %>)" class="recipe-card bg-white rounded-xl shadow-lg border border-emerald-100 p-4 flex flex-col" style="background: linear-gradient(160deg, rgba(255,255,255,0.96), rgba(236,253,245,0.92));">
            <div class="flex items-start justify-between gap-3 mb-2">
              <h3 class="text-lg font-semibold text-emerald-900" style="text-transform: capitalize;"><%= r.title %></h3>
              <% if (typeof r.score === 'number' && r.score > 0) { %>
                <span class="inline-flex items-center px-2 py-1 text-xs font-semibold rounded-full bg-emerald-100 text-emerald-800">Match: <%= r.score %></span>
              <% } %>
            </div>
            <div class="recipe-preview">
              <% if (r.description) { %>
                <p class="text-gray-600 text-sm mb-3"><%= r.description %></p>
              <% } %>
              <p class="text-emerald-600 text-xs font-medium">Click to view cooking steps →</p>
            </div>
            <div class="recipe-instructions">
              <div class="text-xs uppercase tracking-widest text-emerald-600 font-semibold mb-2">Cooking Steps</div>
              <p class="text-gray-700 text-sm mb-3 leading-relaxed whitespace-pre-line"><%= r.instructions || 'No cooking steps available.' %></p>
            </div>
            <div class="mt-auto space-y-1">
              <% if (r.required && r.required.length) { %>
                <div class="text-xs text-gray-500">Required:
                  <span class="ml-1 space-x-1">
                    <% r.required.forEach(function(i){ %>
                      <span class="inline-block px-2 py-0.5 rounded-full bg-emerald-50 text-emerald-800 ring-1 ring-emerald-100"><%= i %></span>
                    <% }) %>
                  </span>
                </div>
              <% } %>
              <% if (r.optional && r.optional.length) { %>
                <div class="text-xs text-gray-500">Ingredients:
                  <span class="ml-1 space-x-1">
                    <% r.optional.forEach(function(i){ %>
                      <span class="inline-block px-2 py-0.5 rounded-full bg-lime-50 text-emerald-800 ring-1 ring-emerald-100"><%= i %></span>
                    <% }) %>
                  </span>
                </div>
              <% } %>
            </div>
          </article>
        <% }) %>
      </div>
    <% } else { %>
      <div class="bg-white rounded-2xl shadow-lg border border-emerald-100 p-8 text-center text-gray-600" style="background: linear-gradient(160deg, rgba(255,255,255,0.96), rgba(236,253,245,0.92));">
        No matching recipes yet. Add more items to your fridge.
      </div>
    <% } %>
  </main>

  <footer class="max-w-5xl mx-auto px-6 py-8 text-center text-sm text-emerald-900/70">
  </footer>
</body>
</html>


